# Examples

This folder groups the runnable example datasets used by the reliability audit tool.

- `demo/`: the canonical small dataset used by `./run_demo.sh` and the web demo flow
- `benchmark/`: a larger anonymized dataset useful for realistic performance and report checks

If you want to try the tool quickly, start with `demo/`.
If you want to validate behavior on a heavier dataset, use `benchmark/`.
